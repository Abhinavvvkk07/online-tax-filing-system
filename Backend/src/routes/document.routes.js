const express = require('express');
const router = express.Router();

// Placeholder for document routes
router.get('/', (req, res) => {
  res.json({ message: 'Document routes placeholder' });
});

module.exports = router;
